[package]
name = "slumber"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Local-first task handoff and resumption for coding agents"

[dependencies]
anyhow = "1.0"
clap = { version = "4.5", features = ["derive"] }
nix = { version = "0.30", features = ["process", "signal"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1.47", features = ["io-util", "macros", "net", "process", "rt", "signal", "time"] }
